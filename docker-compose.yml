services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: weekreport
      POSTGRES_PASSWORD: weekreport
      POSTGRES_DB: weekreport
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U weekreport -d weekreport"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+psycopg://weekreport:weekreport@postgres:5432/weekreport
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: dev-secret-change-me
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-scj8iD_Af6Nf9T88DiKphNKhtc_9iZFpsV5i63OPhg0=}
      CORS_ORIGINS: http://localhost:8080,http://localhost:5173
      FEATURE_LLM: "false"
      FEATURE_EXTERNAL_TELEMETRY: "false"
      ALLOW_PUBLIC_OAUTH: "true"
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    entrypoint: ["/bin/sh", "-c"]
    command: ["exec celery -A app.celery_app worker -l INFO"]
    environment:
      DATABASE_URL: postgresql+psycopg://weekreport:weekreport@postgres:5432/weekreport
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: dev-secret-change-me
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-scj8iD_Af6Nf9T88DiKphNKhtc_9iZFpsV5i63OPhg0=}
      CORS_ORIGINS: http://localhost:8080,http://localhost:5173
      FEATURE_LLM: "false"
      FEATURE_EXTERNAL_TELEMETRY: "false"
      ALLOW_PUBLIC_OAUTH: "true"
    depends_on:
      api:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  beat:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    entrypoint: ["/bin/sh", "-c"]
    command: ["exec celery -A app.celery_app beat -l INFO"]
    environment:
      DATABASE_URL: postgresql+psycopg://weekreport:weekreport@postgres:5432/weekreport
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: dev-secret-change-me
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-scj8iD_Af6Nf9T88DiKphNKhtc_9iZFpsV5i63OPhg0=}
      FEATURE_LLM: "false"
      FEATURE_EXTERNAL_TELEMETRY: "false"
      ALLOW_PUBLIC_OAUTH: "true"
    depends_on:
      api:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api

volumes:
  pgdata:
